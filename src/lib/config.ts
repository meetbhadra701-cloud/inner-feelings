/**
 * Site configuration.
 *
 * To use the shop's real logo: drop a logo file into /public (e.g. logo.svg or
 * logo.png) and change the one line below. The current value points at the
 * owner-supplied "if" monogram already in /public.
 */
export const LOGO_SRC = '/logo.jpg'

/** Years-in-business trust figure shown in the hero. */
export const YEARS_IN_BUSINESS = '10+'
